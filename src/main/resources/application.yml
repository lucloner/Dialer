server:
  port: 18081
spring:
  application:
    name: Dialer
  config:
    import: "optional:configserver:"
  cloud:
    config:
      zookeeper:
        connect-string: 192.168.4.251:32770
      server:
        native:
          search-locations: classpath:/config
#        git:
#          uri: https://github.com/spring-cloud-samples/config-repo
#      profile: dev
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
  datasource:
    url: jdbc:h2:mem:mydb
    username: sa
    password: 1234567
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    defer-datasource-initialization: true
info:
  CMD:
    - docker exec ev0 expressvpn
    - docker exec ev expressvpn
  urls:
    - https://youtube.com
    - https://twitter.com
    - https://x.com
    - https://google.com
    - https://t66y.com
  tolerance: 1
